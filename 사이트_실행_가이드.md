# Smart 공사관리 PoC 실행 가이드 (클릭 중심)

## 1) 가장 쉬운 방법(클릭)

### Windows
1. 저장소 폴더에서 `START_SMART_POC.bat` 파일을 더블클릭합니다.
2. 터미널에 `Local: http://localhost:3000`가 보이면 실행 완료입니다.
3. 브라우저에서 아래 주소 중 하나를 클릭/입력합니다.
   - http://localhost:3000
   - http://localhost:3000/status
   - http://localhost:3000/plans
   - http://localhost:3000/submissions

### macOS / Linux
1. 터미널에서 저장소 폴더로 이동합니다.
2. 아래를 1회 실행합니다.
   ```bash
   chmod +x START_SMART_POC.sh
   ```
3. `./START_SMART_POC.sh` 실행
4. 브라우저에서 `http://localhost:3000` 접속

## 2) 명령어로 실행

```bash
npm run poc:start
```

## 3) 종료 방법

실행 중인 터미널에서 `Ctrl + C`


## 4) 환경 준비가 필요한 경우

```bash
npm run poc:bootstrap
```

위 명령이 Node/npm/git 확인 + 의존성 설치 + 빌드 검증까지 자동으로 수행합니다.
