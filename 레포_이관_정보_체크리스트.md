# 새 레포지토리 이관을 위해 필요한 정보 체크리스트

아래 정보만 주시면 현재 작업물을 새 레포지토리로 거의 그대로 옮길 수 있습니다.

## 1) 필수 정보

1. 새 GitHub 저장소 URL
   - 예: `https://github.com/<id>/<new-repo>.git`
2. 기본 브랜치명
   - `main` 또는 `master`
3. 이관 대상 브랜치명
   - 예: `work`를 그대로 사용할지, `main`으로 합칠지
4. 로컬 저장소 실제 경로(PC)
   - 예: `C:\Users\<사용자명>\Documents\GitHub\<new-repo>`
5. 덮어쓰기 허용 여부
   - 새 레포에 기존 파일이 있을 경우 덮어써도 되는지

## 2) 권장 정보(있으면 작업이 빨라짐)

6. GitHub Desktop에서 현재 선택된 저장소 이름(정확히)
7. 원격 저장소 권한 상태
   - 본인 계정으로 Push 가능한지
8. 커밋 작성자 정보
   - name / email
9. 제외하고 싶은 파일/폴더
   - 예: `export/`, `node_modules/`, 개인 환경파일

## 3) 제가 바로 이관할 때 사용하는 절차(참고)

```bash
# 1) 새 원격 등록(또는 변경)
git remote set-url origin <NEW_REPO_URL>

# 2) 현재 브랜치 push
git push -u origin <TARGET_BRANCH>
```

인증 이슈가 있으면 GitHub Desktop에서 Push를 누르거나,
터미널에서 GitHub 로그인을 먼저 완료하면 됩니다.

## 4) 가장 빠른 답변 템플릿 (복붙용)

```text
새 레포 URL: 
기본 브랜치(main/master): 
이관 브랜치명: 
내 로컬 경로: 
기존 파일 덮어쓰기 허용 여부(예/아니오): 
Push 권한 확인 여부(예/아니오): 
제외 파일/폴더(없으면 없음): 
```
